// @ts-check
import antfu from '@antfu/eslint-config'

export default antfu(
  {
    typescript: true,
    ignores: [
      // eslint ignore globs here
    ],
  },
  {
    rules: {
      // overrides
    },
  },
)
